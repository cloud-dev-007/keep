version: "3.8"

services:
  web:
    build: .
    container_name: qengine
    ports:
      - "8000:8000"
    environment:
      - UVICORN_CMD=uvicorn main:app --host 0.0.0.0 --port 8000
    command: ${UVICORN_CMD}
    volumes:
      - .:/app
    restart: always
